{
  "name": "pii-scrubber-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "PII Assignment Technical Artifact",
  "main": "index.js",
  "workspaces":[
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server & npm run dev --workspace client",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm install && npm run build --workspace client",
    "start": "NODE_ENV=production npm run start --workspace server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
